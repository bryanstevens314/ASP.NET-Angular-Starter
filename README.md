# ASP.NET-Angular-Starter

Utilizes a .NET Core server to host an Angular front end. This is a basic starter project.

## Development server

Run `dotnet run` for a dev server. Navigate to `http://localhost:5000'
